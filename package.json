{
  "name": "thothscript-rewrite",
  "productName": "ThothScript",
  "version": "0.3.1",
  "description": "ThothScript v0.3.1 Stability Pass: clean print, safer replace, large-file guards, session/recovery cleanup, and UI polish.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "npx electron-packager . ThothScript --platform=win32 --arch=x64 --overwrite"
  },
  "devDependencies": {
    "electron": "^30.0.0",
    "electron-packager": "^17.1.2"
  }
}
